{

  "name": "myapp",
  "version": "1.0.0",
  "description": "Esempio app Node.js per esercizio CI/CD OpenShift",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:dev": "NODE_ENV=development node server.js",
    "health": "node -e \"require('http').get('http://localhost:'+(process.env.PORT||8080)+'/healthz',res=>process.exit(res.statusCode===200?0:1)).on('error',()=>process.exit(1))\""
  },
  "dependencies": {
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}